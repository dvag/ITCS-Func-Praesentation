export declare class DragController<T> {
  private startDragValueProvider;
  private onUpdate;
  private onStartDrag;
  private onStopDrag;
  private startValue;
  private startX;
  private startY;
  private startBodyUserSelect;
  private moveListener;
  private upListener;
  private usePointer;
  private touchIdentifier;
  constructor(startDragValueProvider: (startX?: number, startY?: number) => T, onUpdate: (event: DragUpdate<T>) => void, onStartDrag?: () => void, onStopDrag?: () => void);
  onPointerDown(e: PointerEvent): void;
  private fireUpdate;
  onTouchStart(e: TouchEvent): void;
}
export interface DragUpdate<T> {
  startValue: T;
  x: number;
  y: number;
  startX: number;
  startY: number;
}
