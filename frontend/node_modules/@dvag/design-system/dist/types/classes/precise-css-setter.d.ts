export declare class PreciseCssSetter {
  private element;
  private specification;
  constructor(element: Element);
  /**
   * Specifies if a [cssClassName] should be [present].
   */
  set(cssClassName: string, present: boolean): PreciseCssSetter;
  /**
   * Applies all rules
   */
  execute(): void;
}
