export declare class ModalScrollHelper {
  static instance(): ModalScrollHelper;
  isBlocked(): boolean;
  block(modalElement: HTMLElement, scrollElement?: HTMLElement): void;
  unblock(modalElement: HTMLElement): void;
}
