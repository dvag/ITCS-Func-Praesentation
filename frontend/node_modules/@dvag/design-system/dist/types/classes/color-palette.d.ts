export declare class ColorPalette {
  private useNewColors;
  constructor(useNewColors?: boolean);
  getColorCode(colorName: string): string;
  private getDXColorCode;
  private getDSColorCode;
  private getErrorMessage;
  getWheelColor(index: number): string;
  getWheelColorCode(index: number): string;
}
