import { Point } from "./math-util";
export declare class PositionObserver {
  private elementRef;
  private callback;
  private endLoop;
  private position;
  private onWindowChangeEvent;
  private windowEvents;
  constructor(elementRef: HTMLElement, callback: (rect: Point) => void);
  checkLoop(): void;
  private detectChanges;
  disconnect(): void;
}
