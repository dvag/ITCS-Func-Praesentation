/**
 * Diese Funktion extrahiert den Path aus einem Event.
 * Da es in Chrome und Firefox diesbezüglich Unterschiede gibt, wird
 * diese Funktion zur Verfügung gestellt.
 */
export declare function extractPath(event: Event): EventTarget[];
export declare function isDigit(event: KeyboardEvent): boolean;
export declare function isLetter(event: KeyboardEvent): boolean;
export declare function isSpace(event: KeyboardEvent): boolean;
export declare class TransitionStartHelper {
  private context;
  private readonly handle;
  private registeredOnElement;
  /**
   * A class which manages registration and unregistration to the currently JSX unsupported "onTransitionStart" event.
   *
   * @param context: The `this` context which should be used within the handle function.
   * @param handle The events and their corresponding handle functions.
   */
  constructor(context: unknown, handle: (e: TransitionEvent) => void);
  on(element: HTMLElement): void;
  disconnect(): void;
}
