import { ComponentInterface, EventEmitter } from "../../../stencil-public-runtime";
/**
 * Manages tabs within cards to display different content within a `<ds-card>` element by clicking on tabs.
 *
 * @path /Card
 * @name Card Tab-Leiste
 * @shortname Tabs
 * @icon columns
 * @stable
 *
 * @slot - `<ds-card-tab-page>` elements. A tab will be generated for each of them.
 * @slot-example simple
 */
export declare class DsCardTabBar implements ComponentInterface {
  private tabPagesObserver;
  self: HTMLDsCardTabBarElement;
  tabPages: TabPage[];
  /**
   * Displays the tab page which `name` property value corresponds to this property.
   *
   * @value string: `name` of the `<ds-card-tabe-page>` to display.
   *
   * @example complex
   */
  show: string;
  showChanged(): void;
  /**
   * This event will be delegated when the tab switches.
   * It contains the `name` of the current `<ds-card-tab-page>`.
   *
   * @example show-change
   */
  showChange: EventEmitter<string>;
  connectedCallback(): void;
  disconnectedCallback(): void;
  private updateSlotVisibility;
  private setActiveTab;
  render(): any;
  private renderHeaderItems;
}
interface TabPage {
  name: string;
  label: string;
  element: HTMLDsCardTabPageElement;
}
export {};
