import { SearchAutocompleteValue } from "./ds-search";
export declare class SearchAutoCompleteHandler {
  private requestId;
  value: string;
  private internalValues;
  private valuesProvided;
  constructor(requestId: number, value: string);
  provideValues(values: SearchAutocompleteValue[]): void;
  waitForValues(): Promise<number>;
  getValues(): SearchAutocompleteValue[];
}
