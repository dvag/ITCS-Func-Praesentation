import { GridBase, GridInformation } from "./grid-models";
export declare class GridCssBuilder {
  build(base: GridBase, grid: GridInformation, numberOfChildNodes: number): GridCssBuilderResult;
  private processRow;
}
interface GridCssBuilderResult {
  css: string;
  error: string;
}
export {};
