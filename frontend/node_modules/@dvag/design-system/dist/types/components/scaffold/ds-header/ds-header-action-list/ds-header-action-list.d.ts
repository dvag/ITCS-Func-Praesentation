import { ComponentInterface } from "../../../../stencil-public-runtime";
import { HeaderActionConfiguration } from "../ds-header-action/ds-header-action";
/**
 * Configures an openable list in the user-menu.
 *
 * @name Header Action list
 * @path /Scaffold/Header/Nutzer
 */
export declare class DsHeaderActionList implements ComponentInterface {
  self: HTMLDsHeaderActionListElement;
  /**
   * Label for the menu.
   *
   * @value string: E.g: Settings
   *
   */
  label: string;
  /**
   * Opens and closes the menu.
   *
   * @value true: Menu is open
   * @value false: Menu is closed
   */
  open: boolean;
  private childObserver;
  private children;
  private toggleOpen;
  /**
   * @internal
   */
  toHeaderActionListConfiguration(): Promise<HeaderActionListConfiguration>;
  connectedCallback(): void;
  disconnectedCallback(): void;
}
export interface HeaderActionListConfiguration {
  id?: string;
  label: string;
  open: boolean;
  toggleOpen: () => void;
  children: Array<HeaderActionListConfiguration | HeaderActionConfiguration>;
}
