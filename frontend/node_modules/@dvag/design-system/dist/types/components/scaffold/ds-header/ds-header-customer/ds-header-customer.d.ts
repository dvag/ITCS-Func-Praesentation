/**
 * Konfiguriert die Kunden-Integration im Header.
 * @name Header Kundenkonfiguration
 * @shortname Kunde
 * @path /Scaffold/Header
 */
export declare class DsHeaderCustomer {
  /**
   * Customer name
   * @value string: first name and last name
   */
  name: string;
  /**
   * The type of the customer e.g: Kunde
   * @value string: The type of the customer
   */
  typelabel: string;
  /**
   * @internal
   */
  toHeaderCustomerConfiguration(): Promise<HeaderCustomerConfiguration>;
}
export interface HeaderCustomerConfiguration {
  name: string;
  typeLabel: string;
}
