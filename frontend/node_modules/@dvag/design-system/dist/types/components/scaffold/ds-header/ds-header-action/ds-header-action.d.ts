import { EventEmitter } from "../../../../stencil-public-runtime";
/**
 * A clickable item in the header menu.
 * @name Header Action
 * @shortname Action
 * @path /Scaffold/Header/Nutzer
 */
export declare class DsHeaderAction {
  self: HTMLDsHeaderActionElement;
  /**
   * The label for this entry.
   * @value string: E.g: settings
   */
  label: string;
  /**
   * Highlights this entry
   * @value true: Entry highlighted
   * @value false: Entry not highlighted
   */
  highlight: boolean;
  /**
   * Fired when user interacts with this item.
   *
   * Event contains the label attribute of the action.
   */
  interaction: EventEmitter<string>;
  emitEvent(): void;
  /**
   * @internal
   */
  toHeaderActionConfiguration(): Promise<HeaderActionConfiguration>;
}
export interface HeaderActionConfiguration {
  id?: string;
  label: string;
  highlight: boolean;
  oninteraction: () => void;
}
