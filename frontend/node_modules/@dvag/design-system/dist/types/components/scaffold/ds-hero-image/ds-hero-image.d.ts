import { ComponentInterface } from "../../../stencil-public-runtime";
/**
 * Shows images below the `<ds-header>`. The component is responsive and disappears automatically when the smalles breakpoint (<= 768px) is reached.
 *
 * @path /Scaffold
 * @name Hero image
 * @shortname Hero
 * @stable
 */
export declare class DsHeroImage implements ComponentInterface {
  private resizeObserver;
  private breakpointAware;
  self: HTMLDsHeroImageElement;
  /**
   * Url for the picture that should be displayed.
   *
   * @value string: Image-url
   *
   * @example example
   */
  url: string;
  hidden: boolean;
  currentWidth: number;
  connectedCallback(): void;
  disconnectedCallback(): void;
  private initializeResizeObserver;
  render(): any;
}
