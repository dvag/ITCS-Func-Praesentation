import { ComponentInterface, EventEmitter } from "../../../stencil-public-runtime";
/**
 * Data component to describe chart content.
 *
 * @name Chart-Data
 * @path /Visualization
 * @icon qrcode
 * @stable
 */
export declare class DsChartSample implements ComponentInterface {
  /**
   * Label of the data.
   */
  label: string;
  /**
   * Numeric representation of the data value.
   *
   * @value number: The numeric representation.
   */
  value: number;
  /**
   * Formatted representation of the value.
   *
   * @value string: Formatted value.
   */
  formattedValue?: string;
  /**
   * The color of the corresponding chart segment.
   *
   * @value string: The color code.
   * @important
   */
  color: string;
  /**
   * Triggers when value of the sample changes.
   *
   * @value number: Delegates the new value.
   */
  valueChange: EventEmitter<number>;
  onValueChange(newValue: number, oldValue: number): void;
  /**
   * Maps the component into a JS object.
   */
  toChartSample(): Promise<ChartSample>;
  componentShouldUpdate(): boolean | void;
}
export interface ChartSample {
  label: string;
  value: number;
  formattedValue: string;
  color: string;
}
