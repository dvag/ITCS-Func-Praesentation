export declare class InputController {
  private minValue;
  private maxValue;
  private disabled;
  private formatter;
  maxCharacters: number;
  inputElement: HTMLInputElement;
  onFinished: () => void;
  private lastValue;
  private valueBeforeFocus;
  constructor(minValue: number, maxValue: number, disabled: boolean, formatter: (value: string) => string, maxCharacters: number);
  focus(): void;
  setValue(value: number | string): void;
  private setInputElementValue;
  private internalSetValue;
  sendInput(input: string): void;
  preventInput(input: string, inputType: string): boolean;
  onInput(e: InputEvent): void;
  get formattedValue(): string;
  getDisabled(): boolean;
  setDisabled(disabled: boolean): void;
  private finish;
  private format;
  isEmpty(): boolean;
  clearInput(): void;
  onFocus(): void;
  onBlur(): void;
  setInputElement(el: HTMLInputElement): void;
  incrementValue(): void;
  decrementValue(): void;
}
