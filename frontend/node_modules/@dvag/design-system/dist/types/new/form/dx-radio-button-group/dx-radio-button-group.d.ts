import { ComponentInterface, EventEmitter } from "../../../stencil-public-runtime";
/**
 * Form element which offers multiple options in the form of `<dx-radio-button>` elements.
 * Only one option can be selected at a time.
 *
 * @path /Form
 * @name Radio-Button-Group
 * @shortname Radio
 * @icon object-group
 * @stable
 *
 * @slot - One or more `<dx-radio-button>` elements.
 * @slot error-message - This slot can be defined with a `<dx-error-message>` element.
 */
export declare class DxRadioButtonGroup implements ComponentInterface {
  private shouldEmitValueChange;
  self: HTMLDxRadioButtonGroupElement;
  radioButtons: HTMLDxRadioButtonElement[];
  /**
   * Label which is displayed above the radio group.
   * @important
   * @value string: Label e.g `Choose an option`
   * @example label
   */
  label?: string;
  /**
   * @internal
   */
  radiogroupid: string;
  /**
   * Defines if the group is in a read-only state.
   * The label of the current selected radio button will be displayed.
   *
   * @value false: The component is displayed normally.
   * @value true: The current selected option is displayed as text. If no option is selected no text will be displayed.
   * @example readonly
   */
  readonly: boolean;
  /**
   * Defines if the radio button group is a required form element.
   * @value false: It is not required to select an option.
   * @value true: To fulfill the requirement one option has to be selected.
   * @example required
   */
  required: boolean;
  /**
   * Indicates that the form is synced with the KI.
   * @value true: Synchronized.
   * @value false: Not synchronized.
   * @example kisynced
   */
  kisynced: boolean;
  /**
   * A text which is displayed in an error popup below the component.
   * @value string: Error text
   * @example error
   */
  errormessage?: string;
  /**
   * Disables the ability to interact with this component.
   * Every option inside will also be set to `disabled`.
   *
   * @value false: User can select an option.
   * @value true: User cannot select an option.
   *
   * @example disabled
   */
  disabled: boolean;
  onDisabledChange(): void;
  /**
   * This property mirrors the current selected option.
   * If set, an inner radio button with the same `value` has to exist.
   *
   * @value string: The value of an inner radio button.
   * @example value
   * @example read-value
   * @example write-value
   */
  value: string;
  valueChanged(newValue: string, oldValue: string): void;
  /**
   * Delegates the current selected `<dx-radio-button>` element's value.
   * This event will not trigger when setting the `value` in JavaScript.
   *
   * @example read-value
   */
  valueChange: EventEmitter<string>;
  componentWillLoad(): Promise<void> | void;
  onKeyUp(e: KeyboardEvent): Promise<void>;
  onForceUpdateOnChange(e: CustomEvent): void;
  /**
   * Sets the focus on the radio group.
   *
   * If an option is selected it will be focused.
   * If no option is selected the first option will be focused.
   * @example focus
   */
  focusControl(): Promise<void>;
  /**
   * @internal
   */
  select(value: string): Promise<void>;
  connectRadioButton(connectedRadioButton: HTMLDxRadioButtonElement): Promise<void>;
  disconnectRadioButton(disconnectedRadioButton: HTMLDxRadioButtonElement): Promise<void>;
  private postRadioButtonConnect;
  private getRadioButtonsInsertIndex;
  private shouldHandleKeyboardEvent;
  private handleKeyboardArrows;
  private getRadioButtonHolderBeforeSelf;
  private updateCheckedStates;
  private emitValueChange;
  private injectStates;
  private getSelectedRadioButton;
  render(): any;
}
