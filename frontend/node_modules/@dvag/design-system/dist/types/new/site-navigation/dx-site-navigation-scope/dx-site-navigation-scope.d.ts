import { ComponentInterface } from "../../../stencil-public-runtime";
/**
 * A scope tags a set of elements as associated to a certain menu item.
 * The menu item and this scope need to share the same `value` property.
 * For examples see: [Site Navigation](#/component/dx-site-navigation)
 * @name Navigation Scope
 * @shortname Scope
 * @path /Scaffold/Site Navigation
 * @icon map-marked
 * @stable
 */
export declare class DxSiteNavigationScope implements ComponentInterface {
  self: HTMLDxSiteNavigationScopeElement;
  /**
   * Unique value to identify this section scope.
   * @value string: identifier matching the associated menu item
   */
  value: string;
  connectedCallback(): void;
  disconnectedCallback(): void;
  private menu;
  render(): any;
}
