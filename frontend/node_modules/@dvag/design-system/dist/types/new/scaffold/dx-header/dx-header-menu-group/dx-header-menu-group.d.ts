import { ComponentInterface } from "../../../../stencil-public-runtime";
import { MenuGroup, MenuItemElement } from "../header-menu.util";
/**
 * A component to group multiple menu items in the header menu.
 *
 * @name Header Menu Group
 * @shortname Group
 * @path /Scaffold/Header/Menu
 * @icon object-group
 * @stable
 */
export declare class DxHeaderMenuGroup implements ComponentInterface, MenuGroup {
  self: HTMLDxHeaderMenuGroupElement;
  /**
   * The priority indicates the order in which the entries inside a `<dx-header>` can be shown and hidden.
   * This property does not have any effect when the component is not within a `<dx-header>` element.
   *
   * @value number: The priority of the group.
   * @example priority
   */
  priority: number;
  /**
   * @internal
   */
  internalopen: boolean;
  /**
   * @internal
   */
  getInnerMenuItems(): Promise<MenuItemElement[]>;
  /**
   * @internal
   */
  getDefaultWidth(): Promise<any>;
  render(): any;
}
