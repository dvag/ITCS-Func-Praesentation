import { ComponentInterface, EventEmitter } from "../../../../stencil-public-runtime";
import { BreadcrumbItem } from "./dx-breadcrumb-item/dx-breadcrumb-item";
/**
 * The breadcrumb allows for easy overview and navigation within the applications header.
 *
 * @path /Scaffold/Header
 * @name Breadcrumb
 * @shortname Breadcrumb
 * @icon road
 * @stable
 */
export declare class DxBreadcrumb implements ComponentInterface {
  private itemObserver;
  private measurementResizeObserver;
  private breadcrumbContainer;
  private renderHomeIcon;
  private measurementBox;
  self: HTMLDxBreadcrumbElement;
  items: BreadcrumbItem[];
  totalItemWidth: number;
  renderModel: BreadcrumbItem[];
  /**
   * Fired when the user interacts with this item.
   *
   * @example navigation
   */
  navigateHome: EventEmitter<void>;
  /**
   * Fired when the user interacts with a breadcrumb item.
   * As event payload (`event.detail`) the value of the interacted item is supplied.
   *
   * @example navigation
   */
  navigate: EventEmitter<string>;
  connectedCallback(): void;
  componentDidRender(): void;
  disconnectedCallback(): void;
  onResize(): void;
  private collectOptions;
  private updateModel;
  private calculateRemainingItems;
  private measureBreadcrumbParts;
  private calculateItemWidth;
  private setMeasurementBox;
  private renderBreadcrumb;
  private renderBreadcrumbItems;
  private renderBreadcrumbItem;
  private renderBreadcrumbItemText;
  private renderHomePart;
  private renderDots;
  private renderChevron;
  render(): any;
}
