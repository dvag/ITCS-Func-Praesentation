import { EventEmitter } from "../../../../stencil-public-runtime";
import { MenuItemElement } from "../header-menu.util";
/**
 * Menu bar which holds multiple menu items within the header.
 * @name Header Menu Bar
 * @shortname Menu Bar
 * @path /Scaffold/Header
 *
 * @internal
 */
export declare class DxHeaderMenuBar {
  private menuItems;
  private menuItemsObserver;
  private contentSwitcher;
  private sidebarContentOutputs;
  private lastPossibleAvailableSpace;
  private navigationValueToShowInNavigationMenu;
  self: HTMLDxHeaderMenuBarElement;
  navigationMenuItem: MenuItemElement;
  navigationInSlot: boolean;
  showSidebar: boolean;
  onShowSidebarChanged(): void;
  sidebarOpenChange: EventEmitter<boolean>;
  connectedCallback(): void;
  disconnectedCallback(): void;
  componentWillRender(): Promise<void>;
  onForceSidebarContentUpdate(): void;
  handleOpenSidebar(e: CustomEvent<boolean>): void;
  onMenuInteraction(e: CustomEvent<void>): void;
  showChildrenOnSidebarOpen(): void;
  /**
   * @internal
   */
  updateChildren(possibleAvailableSpace: number): Promise<void>;
  private updateSpacerVisibility;
  private getSpacerToTheRight;
  private getAllSlottedMenuItemElements;
  private handleSlottedNavigationItem;
  private menuItemHasSidebarContent;
  private menuLinkHasNavigationElement;
  private getGroupedHeaderMenuEntries;
  private renderHeaderNavigation;
  render(): any[];
}
