import { ComponentInterface } from "../../../../stencil-public-runtime";
import { MenuEntry } from "../header-menu.util";
/**
 * Wraps header item functionality around a custom element which can be placed into the header.
 *
 * @name Header Menu Custom
 * @shortname Custom
 * @path /Scaffold/Header/Menu
 * @icon swatchbook
 *
 * @stable
 */
export declare class DxHeaderMenuCustom implements ComponentInterface, MenuEntry {
  self: HTMLDxHeaderMenuCustomElement;
  /**
   * The priority indicates the order in which the entries can be shown and hidden.
   * This property does not have any effect when the component is not within a `<dx-header>` element.
   *
   * @value number: The priority of the user menu item.
   * @example priority
   */
  priority: number;
  /**
   * @internal
   */
  internalopen?: boolean;
  /**
   * @internal
   */
  getDefaultWidth(): Promise<number>;
  render(): any;
}
