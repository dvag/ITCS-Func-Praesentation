import { EventEmitter } from "../../../../stencil-public-runtime";
/**
 * @internal
 */
export declare class DxSidebar {
  private sidebar;
  private transitionStartHelper;
  self: HTMLDxSidebarElement;
  open: boolean;
  openChange: EventEmitter<boolean>;
  componentDidRender(): void;
  disconnectedCallback(): void;
  private setSidebar;
  private handleSidebarTransitionStart;
  private handleSidebarTransitionEnd;
  private isSidebarOpenCloseTransition;
  private close;
  render(): any;
}
