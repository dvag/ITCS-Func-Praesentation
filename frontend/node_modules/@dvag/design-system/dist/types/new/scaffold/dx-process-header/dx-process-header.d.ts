/**
 * @name Process Header
 * @path /Miscellaneous
 * @icon users
 * @stable
 *
 * @slot note - The note which is displayed below the headline
 * @slot people-display - A `<dx-overflow-menu>` component for selecting multiple persons or a readonly text representing the selected persons.
 * @slot context-menu - A `<dx-overflow-menu>` component which holds different context actions.
 *
 * @slot-example all-slots
 * @slot-example note
 * @slot-example people-display
 * @slot-example context-menu
 */
export declare class DxProcessHeader {
  private overflowMenuSlotObserver;
  private breakpointAware;
  private contextMenu;
  private peopleDisplay;
  self: HTMLDxProcessHeaderElement;
  /**
   * A headline text representing the name of the current page or context.
   *
   * @string: A string which must not be undefined or empty.
   *
   * @example headline
   */
  headline: string;
  connectedCallback(): void;
  disconnectedCallback(): void;
  private updateAlignment;
  private renderNote;
  private renderPeopleDisplay;
  private renderContextMenu;
  private renderHeadlineNoteContainer;
  render(): any;
}
