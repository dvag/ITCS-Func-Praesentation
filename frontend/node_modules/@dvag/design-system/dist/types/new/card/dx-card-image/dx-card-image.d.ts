import { ComponentInterface } from "../../../stencil-public-runtime";
/**
 * Displays a image within a `<dx-card>` component.
 *
 * @path /Card
 * @name Card Image
 * @shortname Image
 * @icon image
 * @stable
 */
export declare class DxCardImage implements ComponentInterface {
  private breakpointAware;
  /**
   * Specifies a path to the image file.
   *
   * @value string: The path to the image file.
   */
  url: string;
  /**
   * Defines the fit of the image.
   *
   * @value  proportional: The image is scaled proportional to its width.
   * @value cover: The image is displayed with a set height. The height is set via the `coverheightmq` properties. The visible part of the image can be controlled using the `coverpositionmq` properties.
   */
  fit: "proportional" | "cover";
  /**
   * Defines the height of the image for the MQ1 breakpoint in cover mode using standard css notation.
   * Only has an effect when fit type "cover" is used.
   * @value string: a height in `px` or `%`.
   */
  coverheightmq1?: string;
  /**
   * Defines the height of the image for the MQ2 breakpoint in cover mode using standard css notation.
   * Only has an effect when fit type "cover" is used.
   * @value string: a height in `px` or %.
   */
  coverheightmq2?: string;
  /**
   * Defines the height of the image for the MQ3 breakpoint in cover mode using standard css notation.
   * Only has an effect when fit type "cover" is used.
   * @value string: a height in `px` or `%`.
   */
  coverheightmq3?: string;
  /**
   * Defines the height of the image for the MQ4 breakpoint in cover mode using standard css notation.
   * Only has an effect when fit type "cover" is used.
   * @value string: a height in `px` or %.
   */
  coverheightmq4?: string;
  /**
   * Defines the height of the image for the MQ5 breakpoint in cover mode using standard css notation.
   * Only has an effect when fit type "cover" is used.
   * @value string: a height in `px` or %.
   */
  coverheightmq5?: string;
  /**
   * Defines the position of the image using standard css notation:
   * Only has an effect when fit type "cover" is used.
   *
   * For more information see: https://developer.mozilla.org/en-US/docs/Web/CSS/background-position
   * @value string: a position in % or px.
   */
  coverpositionmq1?: string;
  /**
   * Defines the position of the image using standard css notation:
   * Only has an effect when fit type "cover" is used.
   *
   * For more information see: https://developer.mozilla.org/en-US/docs/Web/CSS/background-position
   * @value string: a position in % or px.
   */
  coverpositionmq2?: string;
  /**
   * Defines the position of the image using standard css notation:
   * Only has an effect when fit type "cover" is used.
   *
   * For more information see: https://developer.mozilla.org/en-US/docs/Web/CSS/background-position
   * @value string: a position in % or px.
   */
  coverpositionmq3?: string;
  /**
   * Defines the position of the image using standard css notation:
   * Only has an effect when fit type "cover" is used.
   *
   * For more information see: https://developer.mozilla.org/en-US/docs/Web/CSS/background-position
   * @value string: a position in % or px.
   */
  coverpositionmq4?: string;
  /**
   * Defines the position of the image using standard css notation:
   * Only has an effect when fit type "cover" is used.
   *
   * For more information see: https://developer.mozilla.org/en-US/docs/Web/CSS/background-position
   * @value string: a position in % or px.
   */
  coverpositionmq5?: string;
  connectedCallback(): void;
  disconnectedCallback(): void;
  render(): any;
}
