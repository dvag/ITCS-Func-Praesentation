/**
 * Base card component.
 *
 * @path /
 * @name Card
 * @icon address-card
 * @stable
 * @slot - Elements which are displayed within the card.
 * @slot header - The `<dx-card-header>` element within this card.
 * @slot-example example
 */
export declare class DxCard {
  private cardImageSlotObserver;
  private tabBarSlotObserver;
  self: HTMLDxCardElement;
  /**
   * Whether the card has a max height of it's parent or it's content.
   * @internal
   */
  maxcontent: boolean;
  containsCardImage: boolean;
  containsTabBar: boolean;
  connectedCallback(): void;
  disconnectedCallback(): void;
  render(): any;
}
