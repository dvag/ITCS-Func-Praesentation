import { ComponentInterface, EventEmitter } from "../../../stencil-public-runtime";
/**
 * Header element for cards.
 *
 * @path /Card
 * @name Card Header
 * @shortname Header
 * @icon heading
 * @stable
 *
 * @slot - Elements within the header. If not explicitly defined all text elements are displayed bold within this header.
 * The slot automatically aligns the content by defining a space between all elements.
 * @slot action - An action slot which can be used to insert a `<dx-checkbox>`, `<dx-toggle>` or any other action into.
 *
 * @slot-example content
 */
export declare class DxCardHeader implements ComponentInterface {
  self: HTMLDxCardHeaderElement;
  /**
   * The label which is displayed on the left side.
   *
   * @value string: The label text.
   *
   * @example label
   */
  label: string;
  /**
   * The icon which is displayed left of the label.
   *
   * @value string: The icon name.
   *
   * @example icon
   */
  icon?: string;
  /**
   * The color of the icon. This has only an effect when the `icon` property is defined.
   *
   * @value string: Name of the color from the color palette.
   *
   * @example icon
   */
  iconcolor?: string;
  /**
   * If true, the header is clickable and shows a chevron icon on the left side.
   * @internal
   */
  collapsable: boolean;
  /**
   * Whether the header chevron points down or up.
   * @internal
   */
  collapsed: boolean;
  /**
   * Triggers when clicking on the header.
   * @internal
   */
  cardHeaderInteract: EventEmitter<void>;
  componentDidRender(): void;
  private renderIcon;
  private renderButtonOverlay;
  private renderChevron;
  private renderActionSlot;
  render(): any;
}
