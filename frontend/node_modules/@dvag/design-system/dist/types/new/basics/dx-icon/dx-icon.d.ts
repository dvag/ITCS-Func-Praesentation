import { ComponentInterface } from "../../../stencil-public-runtime";
/**
 * Displays an icon of the DVAG icon collection.
 *
 * @path /Basics
 * @name Icon
 * @icon icons
 * @stable
 */
export declare class DxIcon implements ComponentInterface {
  /**
   * Icon subject
   * @value string: Name of the icon.
   * @example all-icons
   */
  icon: string;
  onIconChanged(): void;
  /**
   * Defines the size of the icon. Most icons are available in different sizes and with different line widths.
   *
   * If a `<dx-icon>` is used within other components (e.g. a button), the `size` is set automatically in some cases.
   * @value 16: Icon optimized for 16x16
   * @value 24: Icon optimized for 24x24
   * @value 32: Icon optimized for 32x32
   * @value 48: Icon optimized for 48x48
   * @value 64: Icon optimized for 64x64
   * @value 72: Icon optimized for 72x72
   * @example sizes
   */
  size: number;
  /**
   * Overrides the color of an icon. By default a `grey` (`icons`) color is used.
   * @value string: Name of the color (see example for references)
   * @example colors
   */
  color?: string;
  self: HTMLDxIconElement;
  componentDidRender(): void;
  private getSizeFromCssVariables;
  private renderExternalIcon;
  private renderIcon;
  render(): any;
}
