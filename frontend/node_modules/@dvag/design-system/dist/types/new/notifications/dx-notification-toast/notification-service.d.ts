export declare class NotificationService {
  private currentPaddingTop;
  private currentNotificationToasts;
  private resizeObserver;
  private breakpointAware;
  private notificationsTransitionDebounce;
  private notificationCloseHandle;
  constructor();
  private connect;
  private disconnect;
  private getCurrentPaddingTop;
  static instance(): NotificationService;
  addNotificationToast(notification: HTMLDxNotificationToastElement): void;
  private internalCloseNotification;
  private updatePositions;
  private setNotificationsTransition;
}
