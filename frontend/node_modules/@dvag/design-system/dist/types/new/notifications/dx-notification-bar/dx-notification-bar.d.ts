import { ComponentInterface, EventEmitter } from "../../../stencil-public-runtime";
/**
 * Shows a notification with informations to the user.
 * It has to be closed by the user.
 * @path Notifications
 * @name Notification-Bar
 * @icon info-circle
 * @stable
 *
 * @slot content - The content of this notification.
 */
export declare class DxNotificationBar implements ComponentInterface {
  self: HTMLDxNotificationBarElement;
  private closedByUser;
  /**
   * Shows or hides the notification.
   * @value true: Notification is displayed.
   * @value false: Notification is hidden.
   * @example header-integration
   */
  visible: boolean;
  onVisibileChange(): void;
  /**
   * This event is fired, when the user closes the notification via the close button.
   * @example close-notification
   */
  closeNotification: EventEmitter<void>;
  internalVisiblity: boolean;
  componentWillLoad(): void | Promise<void>;
  private openNotificationDelayed;
  private userClosesNotification;
  private emitCloseEvent;
  private handleGrowBoxStateChanged;
  private renderCloseButton;
  render(): any;
}
