import { EventEmitter } from "../../../../stencil-public-runtime";
import { Point } from "../../../../classes/math-util";
import { Draggable } from "../draggable";
/**
 * @internal
 */
export declare class DxSortHandle implements Draggable {
  private dragController;
  self: HTMLDxSortHandleElement;
  /**
   * @internal
   * This property is managed by a parent <dx-sort-item> element and indicates whether this component is draggable or not.
   */
  internaldraggable: boolean;
  /**
   * @internal
   * This property is managed by a parent <dx-sort-item> element and indicates whether this component is currenlty dragged or not.
   */
  internaldragging: boolean;
  /**
   * @internal
   * This event fires when the user begins to drags this component.
   */
  dragBegin: EventEmitter<Point>;
  /**
   * @internal
   * This event fires when the user stops to drags this component.
   */
  dragFinish: EventEmitter<Point>;
  /**
   * @internal
   * This event fires while the user drags this component.
   */
  dragUpdate: EventEmitter<Point>;
  componentWillLoad(): void;
  render(): any;
}
