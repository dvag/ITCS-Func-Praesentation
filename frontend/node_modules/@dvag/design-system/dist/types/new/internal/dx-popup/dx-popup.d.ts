import { ComponentInterface } from "../../../stencil-public-runtime";
import { HorizontalDirection } from "../../../classes/common-types";
/**
 * @internal
 */
export declare class DxPopup implements ComponentInterface {
  private base;
  private bubble;
  private iconWidth;
  self: HTMLDxPopupElement;
  internalArrowPosition: HorizontalDirection;
  internalDirection: HorizontalDirection;
  /**
   * Show info or error popup
   */
  type: "info" | "error";
  /**
   * Is popup displayed?
   */
  show: boolean;
  /**
   * Uses old theme from <ds-popup>
   */
  dstheme: boolean;
  /**
   * Specifies the position of the arrow.
   */
  arrowposition: HorizontalDirection;
  onDirectionChange(): void;
  /**
   * Specifies the width behavior of the popup bubble.
   * @value parent: width will increases until it matches the parent size
   * @value unconstrained: width will increase unconstrained
   */
  contentwidth: "unconstrained" | "parent";
  onResize(): void;
  componentWillLoad(): Promise<void> | void;
  componentDidRender(): Promise<void> | void;
  private updatePopupDirection;
  private availableSpace;
  private calculateOffset;
  private renderPopup;
  render(): any;
}
