import { EventEmitter } from "../../../../stencil-public-runtime";
import { Point } from "../../../../classes/math-util";
/**
 * @hidden
 * A wrapper component which enables a specific element to be dragged around when it uses a `<dx-sort-handle>`.
 */
export declare class DxSortItem {
  private startDragOffset;
  private slotObserver;
  private draggableElements;
  dragElementRect: DOMRect;
  dragElement: HTMLElement;
  onDragElementChanged(): void;
  self: HTMLDxSortElement;
  /**
   * Internal representation of the sort item.
   * @value string: Identificator
   */
  value: string;
  /**
   * @internal
   */
  internaldraggable: boolean;
  onSortableChange(): void;
  /**
   * @internal
   */
  dragging: EventEmitter<Point>;
  /**
   * @internal
   */
  draggingStart: EventEmitter<Point>;
  /**
   * @internal
   */
  draggingEnd: EventEmitter<void>;
  connectedCallback(): void;
  disconnectedCallback(): void;
  onDragBegan(e: CustomEvent<Point>): void;
  onDragFinished(): void;
  onWindowMouseMove(e: CustomEvent<Point>): void;
  private isCurrentlyDragging;
  private updateDragElementPosition;
  private renderPlaceholder;
  private renderSlot;
  private renderContent;
  render(): any;
}
