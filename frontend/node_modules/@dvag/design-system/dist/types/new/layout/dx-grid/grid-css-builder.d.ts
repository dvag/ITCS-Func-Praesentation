import { GridBase, GridBreakpoint, GridInformation } from "./grid-models";
export declare class GridCssBuilder {
  build(base: GridBase, grid: GridInformation, currentBreakpoint: GridBreakpoint, numberOfChildNodes: number, rowGap: number): GridCssBuilderResult;
  private processRow;
}
interface GridCssBuilderResult {
  css: string;
  error: string;
}
export {};
