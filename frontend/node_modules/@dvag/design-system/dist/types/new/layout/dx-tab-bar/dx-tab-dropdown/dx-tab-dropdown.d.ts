import { EventEmitter } from "../../../../stencil-public-runtime";
import { NewDropdownOption } from "../../../form/dx-dropdown/dx-dropdown-option/dx-dropdown-option";
import { StringOrObject } from "../../dx-list/list.util";
import { ItemSelectionPayload } from "../../dx-list/dx-list-flyout/dx-list-flyout";
/**
 * A dropdown for usage in small media queries in <dx-tab-bar>.
 * @internal
 */
export declare class DxTabDropdown {
  private optionsObserver;
  private selectedOption;
  self: HTMLDxTabDropdownElement;
  label: string;
  internalOpen: boolean;
  options: NewDropdownOption[];
  valueComplete: EventEmitter<StringOrObject>;
  onItemSelectionChanged(e: CustomEvent<ItemSelectionPayload>): void;
  connectedCallback(): void;
  disconnectedCallback(): void;
  private collectOptions;
  /**
   * Delegates keyboard events to the underlying list element.
   * The list element itself cannot receive keyboard events because it has a tabIndex of -1.
   */
  private onInputKeyDown;
  private onInputKeyUp;
  private closeListAndEmitSelectedOption;
  private renderItemList;
  private renderBaseSlot;
  private renderOpenListButton;
  private renderCurrentValue;
  render(): any;
}
