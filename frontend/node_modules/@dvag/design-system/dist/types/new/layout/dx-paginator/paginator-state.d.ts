export declare class PaginatorState {
  private page;
  private count;
  private onStateChange?;
  private previousState;
  constructor(page: number, count: number, onStateChange?: (stateChangePayload: PaginatorStateChangePayload, previousStateChangePayload?: PaginatorStateChangePayload) => void);
  toTheLeft(): void;
  toTheRight(): void;
  setPage(page: number): void;
  build(): PaginatorItem[];
}
export declare type PaginatorItem = "active" | "small" | "medium" | "large";
export interface PaginatorStateChangePayload {
  page: number;
  count: number;
  items: PaginatorItem[];
  movedToLeft: boolean;
}
