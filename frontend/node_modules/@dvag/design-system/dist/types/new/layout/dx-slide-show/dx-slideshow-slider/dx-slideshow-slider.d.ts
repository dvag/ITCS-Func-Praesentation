import { ComponentInterface, EventEmitter } from "../../../../stencil-public-runtime";
/**
 * Form element to select a numeric value within the range.
 *
 * @internal
 * @path /Form
 * @name Slider
 * @icon sliders-h
 * @stable
 */
export declare class DxSlideshowSlider implements ComponentInterface {
  private dragController;
  private knob;
  private initDragOutsideOfKnob;
  self: HTMLDxSlideshowSliderElement;
  internalValue: number;
  isDragging: boolean;
  /**
   * Maximum value of the range, the slider can have.
   * @value number: Maximum value of the slider.
   * @example min-max-value
   */
  max: number;
  /**
   * Size of the Steps, the values can be selected.
   * @value number: Minimal possible change.
   * @example step
   */
  step: number;
  /**
   * Current value of the slider.
   * @value number: Current value
   * @example min-max-value
   */
  value: number;
  onValueChanged(): void;
  /**
   * User made changes. Contains the chosen value.
   * @example value-change
   */
  valueChange: EventEmitter<number>;
  dragging: EventEmitter<boolean>;
  componentWillLoad(): Promise<void> | void;
  componentShouldUpdate(_: unknown, __: unknown, propName: string): boolean | void;
  private onStartDragging;
  private onStopDragging;
  private getKnobWidthPercentage;
  private getKnobPositionPercentage;
  render(): any;
}
