export declare class SwipeController<T> {
  private startSwipeValueProvider;
  private onStart;
  private onSwipeRecognized;
  private onUpdate;
  private currentlySwiping;
  private startValue;
  constructor(startSwipeValueProvider?: () => T, onStart?: () => void, onSwipeRecognized?: (swipeDirection: SwipeDirection) => void, onUpdate?: (updateDate: SwipeUpdateData<T>) => void);
  onTouchStart(e: TouchEvent): void;
}
export declare type SwipeDirection = "left" | "right" | "top" | "bottom" | "none";
export interface SwipeUpdateData<T> {
  startValue: T;
  startX: number;
  startY: number;
  startTime: number;
  currentX: number;
  currentY: number;
  currentTime: number;
}
