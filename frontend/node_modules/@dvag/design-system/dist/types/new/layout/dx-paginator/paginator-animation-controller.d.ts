export declare class PaginatorAnimationController {
  private elements;
  private paginationState;
  private queue;
  private timeoutHandle;
  private animationDuration;
  constructor(page: number, count: number);
  initialize(): void;
  private updateAnimationDuration;
  private updateElementPositions;
  private onStateChanged;
  private processStateChanged;
  private shift;
  setPage(page: number): void;
  setIndex(index: number, element: HTMLElement): void;
}
