import { ComponentInterface, EventEmitter } from "../../../stencil-public-runtime";
import { PaginatorItem } from "./paginator-state";
/**
 * A component which helps to track and visualize pagination.
 *
 * @name Paginator
 * @path /Layout
 * @icon exchange-alt
 * @stable
 *
 */
export declare class DxPaginator implements ComponentInterface {
  private paginatorAnimationController;
  private shouldInitializeAnimationController;
  self: HTMLDxPaginatorElement;
  paginatorItems: PaginatorItem[];
  /**
   * The number of pages.
   *
   * @value number: Number of pages.
   * @example count
   */
  count: number;
  onCountChange(): void;
  /**
   * The first page which should be displayed.
   *
   *@value number: A number from 1 to `count`. This property is mutable and will be delegated by the `pageChanged` event.
   *
   * @example current
   */
  current: number;
  currentPage: number;
  onPageChange(): void;
  /**
   * Fires when the `current` property changes.
   */
  pageChanged: EventEmitter<number>;
  componentWillLoad(): Promise<void> | void;
  componentDidRender(): void;
  private shouldDisplayAnimatablePageButtons;
  private definePaginatorAnimationController;
  private setCurrentPage;
  private renderPageButton;
  private renderMovableButtons;
  private renderMovablePage;
  private renderPageButtons;
  render(): any;
}
