'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');
const assert = require('./assert-d8ea75c6.js');

function randomId() {
  return `${new Date().getTime() % 10000000}-${Math.round(Math.random() * 10000000)}`;
}

const dsCardTabPageCss = ":host{display:block}:host:host([hidden]){display:none !important}";

const DsCardTabPage = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    /**
     * Unique identifier of this component within a `<ds-card-tab-bar>` element.
     * The content will be visible when the corresponding `<ds-card-tab-bar>` element's `show` property value equals the `name`.
     *
     * If no value is specified a random id will be generated.
     *
     * @value string: Identifier
     * @example ohne-name
     */
    this.name = randomId();
  }
  render() {
    var _a;
    assert.assert(((_a = this.label) === null || _a === void 0 ? void 0 : _a.length) > 0, () => "<ds-card-tab-page> needs a title by setting the 'label' property");
    return (index.h(index.Host, null, index.h("slot", null)));
  }
};
DsCardTabPage.style = dsCardTabPageCss;

exports.ds_card_tab_page = DsCardTabPage;
