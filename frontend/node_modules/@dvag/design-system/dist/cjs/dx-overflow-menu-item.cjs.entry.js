'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');
const assert = require('./assert-d8ea75c6.js');

const DxOverflowMenuItem = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
  }
  /**
   * @internal
   * Extracts this element's properties into a complex javascript object.
   */
  async toOverflowMenuItem() {
    var _a;
    assert.assert(typeof this.value !== "number", () => `<dx-overflow-menu-item> Property 'value' must not be of type 'number'.`);
    return {
      id: this.self.id,
      icon: this.icon,
      value: (_a = this.value) !== null && _a !== void 0 ? _a : this.label,
      label: this.label,
      selected: this.selected,
      setSelected: (selected) => this.handleSelection(selected),
    };
  }
  handleSelection(selected) {
    this.selected = selected;
  }
  get self() { return index.getElement(this); }
};

exports.dx_overflow_menu_item = DxOverflowMenuItem;
