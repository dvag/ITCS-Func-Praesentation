'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');

const DsHeaderAction = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    this.interaction = index.createEvent(this, "interaction", 7);
    /**
     * Highlights this entry
     * @value true: Entry highlighted
     * @value false: Entry not highlighted
     */
    this.highlight = false;
  }
  emitEvent() {
    this.interaction.emit(this.label);
  }
  /**
   * @internal
   */
  async toHeaderActionConfiguration() {
    return {
      id: this.self.id,
      label: this.label,
      highlight: this.highlight,
      oninteraction: () => this.emitEvent(),
    };
  }
  get self() { return index.getElement(this); }
};

exports.ds_header_action = DsHeaderAction;
