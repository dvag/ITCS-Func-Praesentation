'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');
const queryUtil = require('./query-util-8642f149.js');

const DsFmErgebnisuebersichtTable = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
  }
  /**
   * @internal
   * Creates a js object out of this table.
   */
  async toErgebnisUebersichtTable() {
    const items = Array.from(this.self.querySelectorAll(queryUtil.notHidden("ds-fm-ergebnisuebersicht-item"))).map((item) => item.toErgebnisUebersichtItem());
    return {
      label: this.label,
      items: await Promise.all(items),
    };
  }
  get self() { return index.getElement(this); }
};

exports.ds_fm_ergebnisuebersicht_table = DsFmErgebnisuebersichtTable;
