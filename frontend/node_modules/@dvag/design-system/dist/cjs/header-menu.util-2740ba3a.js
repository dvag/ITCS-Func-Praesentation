'use strict';

const miscUtil = require('./misc-util-63b423bd.js');

function isMenuItem(menuEntry) {
  return (ITEM_FUNCTION_RENDER_SIDEBAR_CONTENT in menuEntry ||
    miscUtil.tagOf(menuEntry) === "dx-header-menu-item");
}
function isMenuGroup(menuEntry) {
  return (GROUP_FUNCTION_GET_INNER_MENU_ITEM in menuEntry ||
    miscUtil.tagOf(menuEntry) === "dx-header-menu-group");
}
function isMenuLink(menuEntry) {
  return (LINK_PROPERTY_CONNECTED_NAVIGATION_VALUE in menuEntry ||
    miscUtil.tagOf(menuEntry) === "dx-header-menu-link");
}
// Helps to make conditions within isMenuItem and isMenuGroup typesafe
const GROUP_FUNCTION_GET_INNER_MENU_ITEM = "getInnerMenuItems";
const ITEM_FUNCTION_RENDER_SIDEBAR_CONTENT = "renderSidebarContent";
const LINK_PROPERTY_CONNECTED_NAVIGATION_VALUE = "connectednavigationvalue";

exports.isMenuGroup = isMenuGroup;
exports.isMenuItem = isMenuItem;
exports.isMenuLink = isMenuLink;
