'use strict';

const index = require('./index-2f41386b.js');

// TODO: Render with icon when accessible
const ReadonlyTextElement = (props) => [
  index.h("dx-input-label", { label: props.label }),
  index.h("dx-text", { type: "ps", color: "headline" }, props.text),
];
const BaselineElement = (props) => props.onlyDiv ? (index.h("div", null)) : (index.h("dx-text", { class: "baseline-text", style: {
    visibility: "hidden",
    width: "0",
    display: "inline-block",
  } }, "b"));

exports.BaselineElement = BaselineElement;
exports.ReadonlyTextElement = ReadonlyTextElement;
