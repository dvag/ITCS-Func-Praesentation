'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');
const assert = require('./assert-d8ea75c6.js');

const dxTabPageCss = ":host{display:block}:host:host([hidden]){display:none !important}";

const DxTabPage = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
  }
  async toTabPage() {
    return {
      value: this.value || this.label,
      label: this.label,
      element: this.self,
    };
  }
  render() {
    var _a;
    assert.assert(((_a = this.label) === null || _a === void 0 ? void 0 : _a.length) > 0, () => "<dx-tab-page> needs a title by setting the 'label' property");
    return (index.h(index.Host, { key: this.value || this.label }, index.h("slot", null)));
  }
  get self() { return index.getElement(this); }
};
DxTabPage.style = dxTabPageCss;

exports.dx_tab_page = DxTabPage;
