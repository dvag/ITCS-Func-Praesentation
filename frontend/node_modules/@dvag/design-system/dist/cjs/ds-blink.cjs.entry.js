'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');

const dsBlinkCss = ":host{display:block}:host:host([hidden]){display:none !important}";

const DsBlink = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    this.height = 50;
    this.auto = false;
    this.time = 2000;
    this.color = "yellow";
  }
  componentWillLoad() {
    if (this.auto) {
      setInterval(() => this.newHeight(), this.time * 3);
    }
  }
  newHeight() {
    return (this.height = 20 + Math.round(Math.random() * 300));
  }
  render() {
    return (index.h(index.Host, { onClick: () => this.newHeight(), style: {
        height: `${this.height}px`,
        background: this.color,
      } }, index.h("slot", null)));
  }
};
DsBlink.style = dsBlinkCss;

exports.ds_blink = DsBlink;
