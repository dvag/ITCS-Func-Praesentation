'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');

const DsAlertButton = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    /**
     * Label color.
     *
     * @value red: rot
     * @value gold: gold
     */
    this.color = "gold";
  }
  /**
   * Extracts this element's properties into a complex javascript object.
   *
   * @internal
   */
  async toAlertButtonData() {
    var _a;
    return {
      label: this.label,
      value: (_a = this.value) !== null && _a !== void 0 ? _a : this.label,
      confirm: this.confirm,
      color: this.color,
      highlight: this.highlight,
      disabled: this.disabled,
      onClick: this.click.bind(this),
    };
  }
  click() {
    this.self.click();
  }
  get self() { return index.getElement(this); }
};

exports.ds_alert_button = DsAlertButton;
