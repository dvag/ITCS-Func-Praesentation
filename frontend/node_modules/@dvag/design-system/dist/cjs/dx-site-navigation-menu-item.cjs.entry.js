'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-2f41386b.js');
const assert = require('./assert-d8ea75c6.js');
const states = require('./states-d5e95c84.js');

const DxSiteNavigationMenuItem = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    /**
     * State of the content section represented by this menu item.
     * @value error: There is an error within content.
     * @value required: There is an essential information needed in order to continue.
     * @value success: Everything is okay within that section.
     * @value none: No state. (default value)
     */
    this.state = "none";
  }
  /**
   * @internal
   */
  async toMenuItemModel() {
    assert.assertPropertyValue("dx-site-navigation-menu-item", "state", this.state, [
      ...states.SITE_NAVIGATION_STATE_ARRAY,
    ]);
    return {
      label: this.label,
      value: this.value,
      state: this.state,
    };
  }
};

exports.dx_site_navigation_menu_item = DxSiteNavigationMenuItem;
