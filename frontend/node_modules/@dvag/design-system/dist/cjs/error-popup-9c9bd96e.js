'use strict';

const index = require('./index-2f41386b.js');

const ErrorPopup = (props) => {
  var _a;
  return (index.h("dx-popup", { dstheme: true, type: "error", show: ((_a = props.text) === null || _a === void 0 ? void 0 : _a.length) > 0 && (props.show || props.show === undefined), arrowposition: props.rightAligned ? "right" : "left" },
    index.h("ds-text", null, props.text)));
};

exports.ErrorPopup = ErrorPopup;
