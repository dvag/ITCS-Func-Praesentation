import { r as registerInstance, c as createEvent } from './index-04c470ea.js';

const DxChartSample = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    this.valueChange = createEvent(this, "valueChange", 7);
  }
  onValueChange(newValue, oldValue) {
    if (newValue !== oldValue) {
      this.valueChange.emit(this.value);
    }
  }
  /**
   * Maps the component into a JS object.
   */
  async toChartSample() {
    const exposedValue = Math.max(this.value, 0) || 0;
    return {
      label: this.label,
      icon: this.icon,
      value: exposedValue,
      color: this.color,
      selectedcolor: this.selectedcolor || this.color,
    };
  }
  componentShouldUpdate() {
    if (isNaN(this.value)) {
      return false;
    }
  }
  static get watchers() { return {
    "value": ["onValueChange"]
  }; }
};

export { DxChartSample as dx_chart_sample };
