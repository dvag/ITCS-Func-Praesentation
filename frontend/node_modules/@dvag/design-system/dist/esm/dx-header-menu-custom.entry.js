import { r as registerInstance, a as readTask, h, g as getElement } from './index-04c470ea.js';

const dxHeaderMenuCustomCss = ":host{display:block}:host:host([hidden]){display:none !important}:host .custom-container{display:flex;align-items:center;width:max-content;height:56px}";

const DxHeaderMenuCustom = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    /**
     * @internal
     */
    this.internalopen = true;
  }
  /**
   * @internal
   */
  async getDefaultWidth() {
    return new Promise((resolve) => {
      readTask(() => {
        resolve(this.self.shadowRoot.querySelector(".custom-container").offsetWidth);
      });
    });
  }
  render() {
    return (h("dx-horizontal-grow-box", { open: this.internalopen }, h("div", { class: "custom-container" }, h("slot", null))));
  }
  get self() { return getElement(this); }
};
DxHeaderMenuCustom.style = dxHeaderMenuCustomCss;

export { DxHeaderMenuCustom as dx_header_menu_custom };
