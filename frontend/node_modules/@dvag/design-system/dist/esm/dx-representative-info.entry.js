import { r as registerInstance, h, H as Host } from './index-04c470ea.js';

const dxRepresentativeInfoCss = ":host{display:block;flex-shrink:0;--dx-text-color:var(--dx-header-text-icon-theme-color, #2b2b2b)}:host:host([hidden]){display:none !important}.container{width:max-content;display:flex;align-items:center;height:36px;text-align:right}";

const DxRepresentativeInfo = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    /**
     * Specifies the text before mentioning the name of the representative.
     * @value assistant: Shows "Assistent von: " as name prefix.
     * @value representative: Shows "Vertreter von: " as name prefix.
     * @example type
     */
    this.type = "representative";
  }
  renderText() {
    return (h("dx-text", { type: "Infotext-Small" }, this.type === "assistant" ? ASSISTANT_TEXT : REPRESENTATIVE_TEXT, " ", this.name));
  }
  render() {
    return (h(Host, null, h("div", { class: "container" }, this.renderText())));
  }
};
const ASSISTANT_TEXT = "Assistenz von";
const REPRESENTATIVE_TEXT = "Vertretung von";
DxRepresentativeInfo.style = dxRepresentativeInfoCss;

export { DxRepresentativeInfo as dx_representative_info };
