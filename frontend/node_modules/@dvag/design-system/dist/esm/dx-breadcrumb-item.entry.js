import { r as registerInstance } from './index-04c470ea.js';

const DxBreadcrumbItem = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
  }
  /**
   * @internal
   */
  async toItem() {
    var _a;
    return {
      label: this.label,
      value: (_a = this.value) !== null && _a !== void 0 ? _a : this.label,
    };
  }
};

export { DxBreadcrumbItem as dx_breadcrumb_item };
