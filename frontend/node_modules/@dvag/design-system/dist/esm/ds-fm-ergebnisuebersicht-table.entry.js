import { r as registerInstance, g as getElement } from './index-04c470ea.js';
import { n as notHidden } from './query-util-02753b43.js';

const DsFmErgebnisuebersichtTable = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
  }
  /**
   * @internal
   * Creates a js object out of this table.
   */
  async toErgebnisUebersichtTable() {
    const items = Array.from(this.self.querySelectorAll(notHidden("ds-fm-ergebnisuebersicht-item"))).map((item) => item.toErgebnisUebersichtItem());
    return {
      label: this.label,
      items: await Promise.all(items),
    };
  }
  get self() { return getElement(this); }
};

export { DsFmErgebnisuebersichtTable as ds_fm_ergebnisuebersicht_table };
