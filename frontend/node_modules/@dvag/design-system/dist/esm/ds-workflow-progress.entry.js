import { r as registerInstance, h, H as Host } from './index-04c470ea.js';
import { a as assert } from './assert-7fc7463a.js';

const dsWorkflowProgressCss = ":host{display:block;max-width:1440px;margin:0 auto;height:7px;background-color:#e3dac4}:host:host([hidden]){display:none !important}:host .progress{height:100%;background-color:#a7893d;transform-origin:left;transition:transform 300ms}";

const DsWorkflowProgress = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    this.progress = 0;
  }
  render() {
    assert(this.progress >= 0 && this.progress <= 1, () => "<ds-workflow-progress> Die Eigenschaft 'progress' muss zwischen 0.0 und 1.0 liegen.");
    return (h(Host, null, h("div", { class: "progress", style: {
        transform: `scaleX(${Math.max(0, Math.min(this.progress, 1))})`,
      } })));
  }
};
DsWorkflowProgress.style = dsWorkflowProgressCss;

export { DsWorkflowProgress as ds_workflow_progress };
