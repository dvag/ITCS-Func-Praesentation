import { h } from './index-04c470ea.js';

// TODO: Render with icon when accessible
const ReadonlyTextElement = (props) => [
  h("dx-input-label", { label: props.label }),
  h("dx-text", { type: "ps", color: "headline" }, props.text),
];
const BaselineElement = (props) => props.onlyDiv ? (h("div", null)) : (h("dx-text", { class: "baseline-text", style: {
    visibility: "hidden",
    width: "0",
    display: "inline-block",
  } }, "b"));

export { BaselineElement as B, ReadonlyTextElement as R };
