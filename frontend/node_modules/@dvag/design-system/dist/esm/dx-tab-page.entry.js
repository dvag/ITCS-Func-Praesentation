import { r as registerInstance, h, H as Host, g as getElement } from './index-04c470ea.js';
import { a as assert } from './assert-7fc7463a.js';

const dxTabPageCss = ":host{display:block}:host:host([hidden]){display:none !important}";

const DxTabPage = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
  }
  async toTabPage() {
    return {
      value: this.value || this.label,
      label: this.label,
      element: this.self,
    };
  }
  render() {
    var _a;
    assert(((_a = this.label) === null || _a === void 0 ? void 0 : _a.length) > 0, () => "<dx-tab-page> needs a title by setting the 'label' property");
    return (h(Host, { key: this.value || this.label }, h("slot", null)));
  }
  get self() { return getElement(this); }
};
DxTabPage.style = dxTabPageCss;

export { DxTabPage as dx_tab_page };
