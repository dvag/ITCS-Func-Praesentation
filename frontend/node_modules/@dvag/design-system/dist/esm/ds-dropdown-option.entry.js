import { r as registerInstance } from './index-04c470ea.js';
import { a as assert } from './assert-7fc7463a.js';

const dsDropdownOptionCss = ":host{display:block}:host:host([hidden]){display:none !important}";

const DsDropdownOption = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
  }
  /**
   * @internal
   * Extracts this element's properties into a complex javascript object.
   */
  async toOption() {
    var _a;
    assert(typeof this.value !== "number", () => `<ds-dropdown-option> Property 'value' must not be of type 'number'.`);
    return {
      icon: this.icon,
      value: (_a = this.value) !== null && _a !== void 0 ? _a : this.label,
      label: this.label,
      secondaryLabel: this.secondarylabel,
      tertiaryLabel: this.tertiarylabel,
    };
  }
};
DsDropdownOption.style = dsDropdownOptionCss;

export { DsDropdownOption as ds_dropdown_option };
