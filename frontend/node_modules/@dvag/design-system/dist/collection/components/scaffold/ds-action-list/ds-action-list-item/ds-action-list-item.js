import { h, Host } from "@stencil/core";
/**
 * Item for an action list.
 *
 * @path /Scaffold/Action list
 * @name Action list item
 * @shortname Item
 * @icon list-alt
 * @slot label - Content which describes the item. It is displayed on the left side.
 * @slot - One or two text buttons, which are displayed on the right side.
 * @stable
 */
export class DsActionListItem {
  render() {
    return (h(Host, null, h("div", { class: "item-container" }, h("div", { class: "item" }, h("slot", { name: "label" })), h("div", { class: {
        "action-items": true,
      } }, h("slot", null))), h("ds-hr", null)));
  }
  static get is() { return "ds-action-list-item"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["ds-action-list-item.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["ds-action-list-item.css"]
    };
  }
  static get elementRef() { return "self"; }
}
