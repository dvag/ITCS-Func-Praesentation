import { h, Host } from "@stencil/core";
/**
 * Displays a list with multiple items on which you can display different actions on.
 *
 * @path /Scaffold
 * @name Action list
 * @icon list-alt
 * @slot label - Contains the title of the list.
 * @slot - Any number of `<ds-action-list-item>` components.
 * @stable
 */
export class DsActionList {
  render() {
    return (h(Host, null, h("div", { class: "label" }, h("slot", { name: "label" })), h("ds-hr", null), h("div", { class: "item-container" }, h("slot", null))));
  }
  static get is() { return "ds-action-list"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["ds-action-list.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["ds-action-list.css"]
    };
  }
}
