import { Host, h } from "@stencil/core";
/**
 * Content of a card with a default padding.
 *
 * @path /Card
 * @name Card Content
 * @shortname Content
 * @icon square
 * @stable
 * @slot - Elements within the content.
 *
 *
 * @slot-example simple
 */
export class DsCardContent {
  render() {
    return (h(Host, null, h("slot", null)));
  }
  static get is() { return "ds-card-content"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["ds-card-content.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["ds-card-content.css"]
    };
  }
}
