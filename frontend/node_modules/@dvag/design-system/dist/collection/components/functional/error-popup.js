import { h } from "@stencil/core";
export const ErrorPopup = (props) => {
  var _a;
  return (h("dx-popup", { dstheme: true, type: "error", show: ((_a = props.text) === null || _a === void 0 ? void 0 : _a.length) > 0 && (props.show || props.show === undefined), arrowposition: props.rightAligned ? "right" : "left" }, h("ds-text", null, props.text)));
};
