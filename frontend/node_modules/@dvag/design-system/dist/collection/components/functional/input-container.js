import { h } from "@stencil/core";
export const InputContainer = (props, content) => {
  var _a;
  return (h("div", Object.assign({ class: {
      container: true,
      active: props.active,
      "red-outline": props.error,
      "required-outline": props.required,
      "requirement-fulfilled": props.requirementFulfilled,
      "disabled-outline": props.disabled,
    }, title: (_a = props.title) !== null && _a !== void 0 ? _a : "" }, props.custom), content));
};
