import { h, Host } from "@stencil/core";
/**
 * Eine Komponente, die ihre Kind-Elemente zentriert.
 * Wird in Zusammenspiel mit der `ds-centered-board`-Komponente genutzt.
 *
 * @path /Layout/Centered-Board
 * @name Centered-Board Eintrag
 * @shortname Item
 * @icon minus
 * @stable
 * @slot - Die Kindelemente, die zentriert werden sollen.
 */
export class DsCenteredBoardItem {
  render() {
    return (h(Host, null, h("slot", null)));
  }
  static get is() { return "ds-centered-board-item"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["ds-centered-board-item.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["ds-centered-board-item.css"]
    };
  }
}
