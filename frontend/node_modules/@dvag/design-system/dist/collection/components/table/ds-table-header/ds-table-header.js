import { Host, h } from "@stencil/core";
/**
 * This component holds `ds-table-header-item` components and positions them within a row.
 *
 * @path /Table
 * @name Table Header
 * @shortname Header
 * @icon heading
 * @stable
 */
export class DsTableHeader {
  render() {
    return (h(Host, { role: "rowgroup" }, h("div", { class: "header-row", role: "row" }, h("slot", null))));
  }
  static get is() { return "ds-table-header"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["ds-table-header.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["ds-table-header.css"]
    };
  }
}
