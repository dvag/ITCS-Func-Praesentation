import { h, Host } from "@stencil/core";
/**
 * @path /Table
 * @name Table Footer
 * @shortname Footer
 * @icon shoe-prints
 * @stable
 *
 * The table footer. It holds `<ds-table-cell>` components similar to the `<ds-table-row>` component.
 * It is rendered below the table rows and is excluded from the sorting and paging of the table.
 */
export class DsTableFooter {
  render() {
    return (h(Host, { role: "rowgroup" }, h("div", { class: "footer-row", role: "row" }, h("slot", null))));
  }
  static get is() { return "ds-table-footer"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["ds-table-footer.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["ds-table-footer.css"]
    };
  }
}
