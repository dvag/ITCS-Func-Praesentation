import { tagOf } from "../../../classes/misc-util";
export async function extractRadioButtonOutOfEventPath(e) {
  var _a, _b, _c;
  return ((_c = (_b = (_a = e.target).getRadioElement) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : e
    .composedPath()
    .find((element) => tagOf(element) === "dx-radio-button"));
}
