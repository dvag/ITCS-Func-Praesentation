import { h, Host } from "@stencil/core";
/**
 * Displays and collapses content when clicked.
 *
 * @path /Layout
 * @name Accordion Group
 * @icon chevron-double-down
 * @stable
 *
 * @slot - `<dx-accordion>` elements managed by this group
 */
export class DxAccordionGroup {
  render() {
    return (h(Host, null, h("slot", null)));
  }
  static get is() { return "dx-accordion-group"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["dx-accordion-group.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["dx-accordion-group.css"]
    };
  }
  static get elementRef() { return "self"; }
}
