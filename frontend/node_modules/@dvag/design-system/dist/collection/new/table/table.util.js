export function containsExpandableRows(table) {
  return table.querySelector('dx-table-row *[slot="content"]') != null;
}
export function showStickyContainer(tableScrollable, showExpandColumn, showSelectionColumn, mediaQueryAllowsStickyness, isStickySlotDefined) {
  if (!tableScrollable) {
    return false;
  }
  const defaultStickyColumnsVisible = showExpandColumn || showSelectionColumn;
  return (defaultStickyColumnsVisible ||
    (mediaQueryAllowsStickyness && isStickySlotDefined()));
}
