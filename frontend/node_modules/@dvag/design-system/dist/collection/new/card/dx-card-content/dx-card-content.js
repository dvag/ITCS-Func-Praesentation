import { h, Host } from "@stencil/core";
/**
 * Content container for the <dx-card> component with a default padding.
 *
 * @path /Card
 * @name Card Content
 * @shortname Content
 * @icon square
 * @stable
 * @slot - Elements within the content.
 * @slot-example simple
 */
export class DxCardContent {
  render() {
    return (h(Host, null, h("slot", null)));
  }
  static get is() { return "dx-card-content"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["dx-card-content.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["dx-card-content.css"]
    };
  }
}
